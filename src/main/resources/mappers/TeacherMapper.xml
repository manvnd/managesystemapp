<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.managesystem.managesystempackage.repository.before.teacher.TeacherRepository">
    <select id="teacherLogin" parameterType="com.managesystem.managesystempackage.entity.Teacher" resultType="com.managesystem.managesystempackage.entity.Teacher">
        select * from teacher where number = #{number} and pwd = #{pwd}
    </select>
    <insert id="teacherSave" parameterType="com.managesystem.managesystempackage.entity.Teacher">
        insert into teacher(name, number, pwd) values (#{name}, #{number}, #{pwd})
    </insert>
    <insert id="dutyAdd" parameterType="Duty">
        insert into duty(name, summary, descriptionFile, teacherId, size, selectedNumber) values (#{name}, #{summary}, #{descriptionFile} , #{teacherId}, #{size}, #{selectedNumber})
    </insert>
    <select id="getDutyByTeacherId" resultType="Duty">
        select * from duty where teacherId = #{teacherId};
    </select>
</mapper>